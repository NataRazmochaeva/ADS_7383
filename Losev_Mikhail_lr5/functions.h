#pragma once

#include <iostream>
#include <vector>
#include <cstring>
#include <fstream>
#include "bst_interf.h"

using namespace std;

namespace losev_functions
{
	void rand_init();
	bool is_digit( char c );
	int check_for_externs(string str);

}